# Excellent maintainability improvement
def normalize_lowercase(
    value: str,
) -> str:
    return value.strip().lower()


def normalize_uppercase(
    value: str,
) -> str:
    return value.strip().upper()
