# Normalize string value to lowercase format
def normalize_lowercase(
    value: str,
) -> str:

    return value.strip().lower()


# Normalize string value to uppercase format
def normalize_uppercase(
    value: str,
) -> str:

    return value.strip().upper()
